[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npx serve dist -p ${PORT:-3002} -s"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
